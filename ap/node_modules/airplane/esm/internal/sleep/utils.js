// Takes in the start time (ms) and duration (ms)
// And returns the end time as an ISO string.
export const calculateEndTimeISOString = (startTimeMs, durationMs) => {
    return new Date(startTimeMs + durationMs).toISOString();
};
