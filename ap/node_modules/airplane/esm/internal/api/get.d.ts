import { ClientOptions } from "./client";
import { Run, Session } from "./types";
import { SerializedParamValues } from "../params/params_serialized";
export declare const getRun: <TParamValues extends SerializedParamValues = SerializedParamValues, TOutput = unknown>(runID: string, opts?: ClientOptions) => Promise<Run<TParamValues, TOutput>>;
export declare const getSession: <TParamValues extends SerializedParamValues = SerializedParamValues>(sessionID: string, opts?: ClientOptions) => Promise<Session<TParamValues>>;
