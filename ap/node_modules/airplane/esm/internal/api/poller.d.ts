type PollerOptions = {
    delayMs: number;
};
type PollFunction<Output> = () => Output | null | Promise<Output | null>;
export declare class Poller {
    private opts;
    constructor(opts: PollerOptions);
    run<Output = unknown>(fn: PollFunction<Output>): Promise<Output>;
}
export {};
