import { BuiltInParamValues } from "./builtins";
import { ClientOptions } from "../api/client";
import { BuiltInRun } from "../api/types";
export type Contact = {
    email: string;
    name: string;
};
export type EmailOptions = {
    subject?: string;
    message?: string;
    client?: ClientOptions;
};
export type MessageOutput = {
    number_of_recipients: number;
};
export declare const message: (emailResource: string, sender: Contact, recipients: Contact[] | string[], opts?: EmailOptions) => Promise<BuiltInRun<BuiltInParamValues, MessageOutput>>;
