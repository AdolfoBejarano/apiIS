import type { RunStatus, SessionStatus } from "airplane/api";
export declare const isStatusTerminal: (status?: RunStatus | SessionStatus) => boolean;
interface GenericExecuteError {
    message: string;
    statusCode: number;
}
export declare const isGenericExecuteError: (x: any) => x is GenericExecuteError;
export {};
