type Coords = {
    x: number;
    y: number;
};
type DragProps = {
    to?: HTMLElement | Coords;
    delta?: Coords;
    steps?: number;
    duration?: number;
};
export declare function drag(element: HTMLElement, { to: inTo, delta, steps, duration }: DragProps): Promise<void>;
export {};
