import { BaseState, DefaultState } from "../BaseState";
export type DialogState = {
    id: string;
    opened: boolean;
    open: () => void;
    close: () => void;
} & BaseState;
export type InitialDialogState = {
    opened?: boolean;
};
export declare const DefaultDialogState: DefaultState<DialogState>;
