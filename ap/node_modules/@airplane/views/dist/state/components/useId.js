import { useRef } from "react";
let uniqueId = 0;
const getUniqueId = () => uniqueId++;
const useComponentId = (id) => {
  const idRef = useRef();
  if (idRef.current === void 0) {
    idRef.current = `component-${getUniqueId()}`;
  }
  return id || idRef.current;
};
export {
  useComponentId
};
//# sourceMappingURL=useId.js.map
