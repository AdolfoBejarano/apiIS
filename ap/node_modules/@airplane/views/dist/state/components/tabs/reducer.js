const reducer = (state, action) => {
  switch (action.type) {
    case "setValue": {
      const {
        value
      } = action;
      return {
        ...state,
        value
      };
    }
    default:
      throw new Error("invalid action");
  }
};
export {
  reducer
};
//# sourceMappingURL=reducer.js.map
