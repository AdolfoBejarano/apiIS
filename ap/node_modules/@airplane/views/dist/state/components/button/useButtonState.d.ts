import { ButtonState } from "./state";
/**
 * useButton is a hook that creates and manages a Button's state on the
 * Airplane context
 */
export declare const useButtonState: (id: string) => ButtonState;
