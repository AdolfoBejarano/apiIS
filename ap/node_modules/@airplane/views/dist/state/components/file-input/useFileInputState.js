import { useReducer, useMemo } from "react";
import { useStateSetters } from "../input/useStateSetters.js";
import { ComponentType, useSyncComponentState } from "../../context/context.js";
import { reducer } from "./reducer.js";
const useFileInputState = (id, options) => {
  const initialState = {
    value: options.initialState.initialValue,
    disabled: options.initialState.disabled ?? false
  };
  const [internalState, dispatch] = useReducer(reducer, {
    ...initialState,
    showErrors: false,
    errors: []
  });
  const stateSetters = useStateSetters(dispatch, initialState);
  const state = useMemo(() => ({
    ...internalState,
    ...stateSetters,
    id,
    componentType: ComponentType.FileInput
  }), [internalState, stateSetters, id]);
  useSyncComponentState(id, state);
  return {
    state,
    dispatch
  };
};
export {
  useFileInputState
};
//# sourceMappingURL=useFileInputState.js.map
