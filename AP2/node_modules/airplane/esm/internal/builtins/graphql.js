import { convertResourceAliasToID } from "./builtins";
import { executeWithResources } from "../execute";
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const request = async (graphqlResource, operation, opts = {}) => {
    const { variables, headers, urlParams, retryFailures, client } = opts;
    return executeWithResources("airplane:graphql_request", {
        operation,
        variables: variables !== null && variables !== void 0 ? variables : {},
        headers: headers !== null && headers !== void 0 ? headers : {},
        urlParams: urlParams !== null && urlParams !== void 0 ? urlParams : {},
        retryFailures: retryFailures !== null && retryFailures !== void 0 ? retryFailures : false,
    }, { api: convertResourceAliasToID(graphqlResource) }, client);
};
