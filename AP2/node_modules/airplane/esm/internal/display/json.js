import { getRuntime } from "../runtime";
/**
 * Appends a display to the run that renders a JSON payload.
 * @param value JSON payload to render
 */
export const json = async (value) => {
    await getRuntime().display({
        kind: "json",
        value,
    });
};
