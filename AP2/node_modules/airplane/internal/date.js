"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.toISOString = void 0;
/**
 * toISOString converts a Date into ISO format.
 *
 * Compared with Date.toISOString, this retains the timezone instead of converting into UTC.
 * @param d
 * @returns ISO-formatted date string
 */
const toISOString = (d) => {
    const tzo = -d.getTimezoneOffset();
    const sign = tzo >= 0 ? "+" : "-";
    const p = (num) => {
        return (num < 10 ? "0" : "") + num;
    };
    const date = `${d.getFullYear()}-${p(d.getMonth() + 1)}-${p(d.getDate())}`;
    const time = `${p(d.getHours())}:${p(d.getMinutes())}:${p(d.getSeconds())}`;
    const timezone = `${sign}${p(Math.floor(Math.abs(tzo) / 60))}:${p(Math.abs(tzo) % 60)}`;
    return `${date}T${time}${timezone}`;
};
exports.toISOString = toISOString;
